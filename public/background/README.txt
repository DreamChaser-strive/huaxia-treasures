请将背景图片放在此目录下。

会按以下顺序尝试加载（找到第一个存在的即使用，无需重命名）：
  02.png, 01.png, 03.png, bg.jpg, bg.png, bg.webp, image.jpg, image.png, background.jpg, background.png

请通过开发服务器访问页面（npm run dev 或 npx vite），不要直接双击打开 HTML。
